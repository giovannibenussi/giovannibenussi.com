import ToolShell from "./ToolShell";
import { JsonFormatter } from "./components/JsonFormatter";

export default function JsonFormatEntry() {
  return (
    <ToolShell>
      <JsonFormatter />
    </ToolShell>
  );
}
